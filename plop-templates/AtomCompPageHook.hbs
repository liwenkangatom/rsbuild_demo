export type Use{{pascalCase name}}PagePropsType = {
  // hook props type
};
export const use{{pascalCase name}}Page = (props: Use{{pascalCase name}}PagePropsType) => {
  const {} = props;

  return {};
};
